PORT=4000
FRONTEND_ORIGIN=http://localhost:5173,https://planering.netlify.app
# Add a real MongoDB Atlas connection string here for persistent storage.
# If left empty, the backend will fall back to `data/planner.json`.
MONGODB_URI=
MONGODB_DB_NAME=planering
